$object=new objectname;
$object->publicvariable;
$object->publicfunction($variables);

# global variables:
storagehost
storagepassword

wwwdir
wwwurl
wwwtitle

# storage functions
connect($globals)
query_sql('query(for now - normal)')
query_file('file');
